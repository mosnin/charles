"""Marketing & social tools.

The agent posts on the founder's behalf to social channels (Twitter/X,
LinkedIn) and reads back metrics. Mutating posts are wrapped by the
approval gate at the manager layer — adapters here just call the API.
"""
